persistencia productos en máquina local (contenedor en docker) con MySQL
persistencia mensajes en archivo JSON local (file persistence) en "messagePersistenceFile.json"

ejecutar en terminal "npm run st" para corre scipt con archivo de TablaSQL y archivo de server.js (levantar servidor).

