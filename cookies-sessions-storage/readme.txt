ejecutar "node server.js" en terminal de carpeta "backend"

dirección del lado del cliente: "http://localhost:8080/inicio"

Se introduce nombre, al presionar "Login" se informa el nombre ingresado y los datos de sesión.

En posterior se direcciona a la página "index" con le mensaje de bienvenida y el boton de logout. El boton de logout tiene incorporado el destroy. Posterior a presionarlo, se recarga la página y vuelve al /inicio





